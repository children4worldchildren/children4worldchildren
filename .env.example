# Google Maps API Key
VITE_GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here

# Make sure to:
# 1. Create a .env file in the root directory
# 2. Replace 'your_google_maps_api_key_here' with your actual Google Maps API key
# 3. Add the following APIs to your Google Cloud project:
#    - Maps JavaScript API
#    - Geocoding API
#    - Places API

# Note: The .env file is in .gitignore, so it won't be committed to version control
